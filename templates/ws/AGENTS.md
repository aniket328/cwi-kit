<!-- GENERATED by cwi ws meta {{DATE}} — edit ops/manifest.json, not this file -->
# {{NAME}} workspace — agent entry point

Read `_meta.md` in this folder first, then `ops/STATE.md`, `ops/AGENDA.md`, `ops/MEMORY.md`. Inside a code repository, follow that repository's own `AGENTS.md` or `CLAUDE.md`.

Rules: no secret in any file here (the vault in `ops/manifest.json` injects them at run time); work logs in `ops/log/`, decisions in `ops/MEMORY.md`, parked items in `ops/AGENDA.md`; `ops/STATE.md` is overwritten, never appended; `ops/` is committed by the hooks and never left dirty; code repositories are never auto-committed; `_scratch/` is disposable; anything else outside a repository fails `cwi doctor`.
