#!/usr/bin/env python3
"""cwi — CWI Studio workspace tooling. Resolves the kit from its own location, so a symlink works."""
import pathlib
import sys

sys.path.insert(0, str(pathlib.Path(__file__).resolve().parent.parent))
from cwi_kit.cli import main  # noqa: E402

sys.exit(main())
